package dsa.course.dsa

fun main() {
    //Full binary tree,complete binary and binary tree
    // Binary tree: Should have 0 or 1 child at most 2 that is binary tree
    // Full binary tree should have all is child completed at what level the binary e.g totalNumbersOfNode = 2^h - 1
    //Where the h is the level so if the return of total is equal then is full binary tree.
    //Complete binary tree:from h-1 it should be full binary tree and child should be arranged from left to right.
    // Heap:Heap is complete binary tree. heap = full binary tree + complete binary tree

    val newInstanceOFTree = TreeImplementation()

}

class TreeImplementation {

}